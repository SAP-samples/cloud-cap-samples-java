{
  "name": "deploy",
  "dependencies": {
    "@sap/hdi-deploy": "^3"
  },
  "scripts": {
    "postinstall": "cd .. && cds build --for hana",
    "start": "node node_modules/@sap/hdi-deploy --exit"
  }
}
