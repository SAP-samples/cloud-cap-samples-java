{
  "dependencies": {
    "@cap-js/hana": "^3.1.0",
    "@sap/cds": "^10.1.0",
    "@sap/cds-mtxs": "^4",
    "@sap/xssec": "^4.15.0",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^3"
  },
  "engines": {
    "node": "^22"
  },
  "cds": {
    "profiles": [
      "mtx-sidecar",
      "java"
    ]
  },
  "scripts": {
    "start": "cds-serve",
    "build": "cds build ../.. --for mtx-sidecar --production && npm ci --prefix gen"
  }
}
