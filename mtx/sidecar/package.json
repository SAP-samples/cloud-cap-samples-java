{
  "dependencies": {
    "@cap-js/hana": "1.8.1",
    "@sap/cds": "^9",
    "@sap/cds-mtxs": "^3",
    "@sap/xssec": "^4",
    "express": "^4"
  },
  "devDependencies": {
    "sqlite3": "^5"
  },
  "engines": {
    "node": "^22"
  },
  "cds": {
    "profiles": ["mtx-sidecar", "java"],
    "[development]": {
      "requires": { "auth": "dummy" }
    }
  },
  "scripts": {
    "start": "cds-serve",
    "build": "cds build ../.. --for mtx-sidecar --production && cd gen && npm install"
  }
}
