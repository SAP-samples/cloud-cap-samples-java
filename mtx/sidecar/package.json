{
  "dependencies": {
    "@cap-js/hana": "^3.0.1",
    "@sap/cds": "^10.0.3",
    "@sap/cds-mtxs": "^4.0.1",
    "@sap/xssec": "^4.13.3",
    "express": "^4.22.2"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^3.0.2"
  },
  "engines": {
    "node": "^22"
  },
  "cds": {
    "profiles": [
      "mtx-sidecar",
      "java"
    ]
  },
  "scripts": {
    "start": "cds-serve",
    "build": "cds build ../.. --for mtx-sidecar --production && npm ci --prefix gen"
  }
}
