{
  "dependencies": {
    "@cap-js/hana": "^3.0.2",
    "@sap/cds": "^10.0.5",
    "@sap/cds-mtxs": "^4",
    "@sap/xssec": "^4.15.0",
    "express": "^4.22.2"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^3"
  },
  "engines": {
    "node": "^22"
  },
  "cds": {
    "profiles": [
      "mtx-sidecar",
      "java"
    ]
  },
  "scripts": {
    "start": "cds-serve",
    "build": "cds build ../.. --for mtx-sidecar --production && npm ci --prefix gen"
  }
}
