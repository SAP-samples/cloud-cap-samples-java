{
  "dependencies": {
    "@cap-js/hana": "^2.8.0",
    "@sap/cds": "^9.9.1",
    "@sap/cds-mtxs": "^4",
    "@sap/xssec": "^4.13.0",
    "express": "^4.22.2"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2.4.0"
  },
  "engines": {
    "node": "^22"
  },
  "cds": {
    "profiles": [
      "mtx-sidecar",
      "java"
    ]
  },
  "scripts": {
    "start": "cds-serve",
    "build": "cds build ../.. --for mtx-sidecar --production && npm ci --prefix gen"
  }
}
