{
    "authenticationMethod": "route",
    "routes": [
      {
        "source": "(/.*)/com.sap.model.bookshop.CatalogServiceSecured(/.*)",
        "authenticationType": "xsuaa",
        "destination": "backend"
      },
      {
        "source": "^/(.*)$",
        "authenticationType": "none",
        "destination": "backend"
      }
    ]
  }
